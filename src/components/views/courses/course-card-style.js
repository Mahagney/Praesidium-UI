import { makeStyles } from '@material-ui/core/styles';

const useStylesCourseCard = makeStyles({
  card: {
    maxWidth: 370,
    minHeight: 300,
    minWidth: 370
  },
  media: {
    marginTop: '8px',
    height: 140
  }
});

export default useStylesCourseCard;
