import { makeStyles } from '@material-ui/core/styles';

const useStylesCoursesCards = makeStyles({
  container: {
    marginTop: 10,
    padding: 20
  },
  fab: {
    position: 'absolute',
    bottom: '40px',
    right: '40px'
  }
});

export default useStylesCoursesCards;
