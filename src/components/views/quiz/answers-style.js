import { makeStyles } from '@material-ui/core/styles';

const useStylesAnswers = makeStyles(() => ({
  formControl: {
    width: '100%',
    paddingTop: '16px',
  },
  formControlLabel: {
    borderTop: '1px solid #eee',
    padding: '8px 0px 8px 0px',
  },
}));

export default useStylesAnswers;
