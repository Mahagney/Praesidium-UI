import { makeStyles } from '@material-ui/core/styles'

const useStylesQuestionCount = makeStyles((theme) => ({
    questionCount: {
        fontSize: '20px',
        padding: '32px 0px 16px 0px'
    }
}));

export default useStylesQuestionCount;